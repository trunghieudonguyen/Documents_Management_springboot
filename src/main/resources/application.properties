# ======================================
# ? SERVER SETTINGS
# ======================================
server.port=8080
spring.thymeleaf.cache=false

# ======================================
# ?? ORACLE DATABASE
# ======================================
spring.datasource.url=jdbc:oracle:thin:@localhost:1521/XEPDB1
spring.datasource.username=HIEUDNT
spring.datasource.password=Donguyentrunghieu12@
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

# --- Connection Pool (HikariCP) ---
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=10000
spring.datasource.hikari.max-lifetime=30000

# ======================================
# ?? JPA / HIBERNATE
# ======================================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.OracleDialect
spring.jpa.properties.hibernate.default_schema=HIEUDNT
spring.jpa.open-in-view=false

# ======================================
# ? EMAIL (SMTP - GMAIL)
# ======================================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=12c1chuyenphanngochien@gmail.com
spring.mail.password=rztc tymw qucx uwge
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000
spring.mail.default-encoding=UTF-8

# ======================================
# ? SMS API (OPTIONAL - MOCK)
# ======================================
app.sms.api.url=https://dummy-sms-service.com/send
app.sms.api.key=dummy-key-123

# ======================================
# ?? SPRING BOOT GENERAL
# ======================================
spring.main.allow-bean-definition-overriding=true
spring.application.name=DocumentsManagement

# JWT settings
jwt.secret=ChangeThisVerySecretKeyForDevOnly
jwt.expiration-ms=3600000
